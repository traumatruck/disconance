namespace Disconance.Models.Channels;

/// <summary>
///     Represents a sort order type for forum channels.
/// </summary>
public enum ForumSortOrderType
{
    LatestActivity = 0,
    CreationDate = 1
}

/// <summary>
///     Represents a forum layout type for forum channels.
/// </summary>
public enum ForumLayoutType
{
    NotSet = 0,
    ListView = 1,
    GalleryView = 2
}
